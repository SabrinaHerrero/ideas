create database ideas;
use ideas;
create table dates(title varchar(255) NOT NULL, description varchar(2000) NOT NULL, completed boolean NOT NULL, date varchar(2000), id int NOT NULL AUTO_INCREMENT, PRIMARY KEY(project_id));